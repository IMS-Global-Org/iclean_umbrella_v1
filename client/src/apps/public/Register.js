import React, { useState, useEffect } from 'react'
import { connect } from 'react-redux'

const Register = ({dispatch, ...rest}) => {
  return (
    <div>Registration Form</div>
  )
}

export default connect()(Register)
